---
"calligraph": minor
---

Add `variant` prop (`"text"` | `"number"`) for rolling digit transitions, and `animation` prop with spring presets (`"smooth"`, `"snappy"`, `"bouncy"`) inspired by SwiftUI.
